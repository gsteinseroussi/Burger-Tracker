<li>
    {{burger_name}}
    {{#if devour}}<span class="eaten">{{/if}}
        <button class="devour" data-id="{{id}}" data-newdevour="{{devour}}">
            {{#unless devour}}DEVOUR {{/unless}}

        </button>

</li>